import {StyleSheet} from 'react-native';
import {Colors, Metrics, Fonts} from '../../../themes';

const styles = StyleSheet.create({
  backImage: {
    marginLeft: 10,
    marginRight: 80,
  },
  header: {
    fontSize: 30,
    color: Colors.white,
    // fontFamily: Fonts.SFDBold,
    marginLeft: 6,
    marginTop: 5,
    alignSelf: 'center',
  },
});

export default styles;
